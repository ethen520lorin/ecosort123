# Commit 01

`chore: initialise Expo TypeScript project`


# EcoSort Assessment 4

EcoSort is a React Native / Expo mobile application that helps users check local recycling guidance, save a privacy-aware scan history, and demonstrate core mobile development features.

## Run

```bash
npm install
npm start
```

## Android

```bash
npm run android
```

## Notes

- Do not commit `.env` or Firebase credentials.
- Use `.env.example` when Firebase features are introduced.
- Each commit folder is a complete runnable snapshot. In a real Git repository, copy one snapshot into the repository root, commit it, then overwrite it with the next snapshot and commit again.
