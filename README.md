# EcoSort — Commit 02

Commit message: `feat: add bottom navigation and screen layout`

This version is fully runnable and adds the main structure: Home, Search, History, and Settings. 
Run:

```bash
npm install
npm start
```
